title: Missing keys are asked interactively

interactive: true

requested_params:
    boolean:
        default: 'false'
        input: 'true'
    nested:
        default: nested
        input: '{foo: bar, bar: [foo, bonjour, null]}'
    another:
        default: bonjour
        input: 'null'
